local BLD = require "modules.build"

local COLOUR_NORMAL = vmath.vector4(0, 1, 0, 1)
--local COLOUR_NETURAL = vmath.vector4(1, 1, 0, 1)
local COLOUR_FORBIDDEN = vmath.vector4(1, 0, 0, 1)

function init(self)
	msg.post(".", hash("disable"))
end

function on_message(self, message_id, message, sender)
	if message.enter == false then
		go.set("#sprite", "tint", COLOUR_NORMAL)
		BLD.valid_placement = true
	elseif message.enter then
		go.set("#sprite", "tint", COLOUR_FORBIDDEN)
		BLD.valid_placement = false
	end
end